---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/ui/PageHero.astro';
import RequestFormBlock from '../../components/ui/RequestFormBlock.astro';
import { SITE, REQUISITES } from '../../lib/constants';
import { breadcrumbSchema, localBusinessSchema } from '../../lib/seo/schema';

const breadcrumbs = breadcrumbSchema([
  { name: 'Главная', url: SITE.url },
  { name: 'Контакты', url: `${SITE.url}/kontakty/` },
]);
---

<BaseLayout
  title="Контакты"
  description={`Контакты DELDIN TRADE: ${SITE.phone}, ${SITE.email}. ${SITE.workHours}.`}
  path="/kontakty/"
>
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbs)} />
  <script type="application/ld+json" set:html={JSON.stringify(localBusinessSchema())} />

  <PageHero
    title="Контакты"
    subtitle="Свяжитесь с нами удобным способом — ответим в рабочее время"
    breadcrumb={[{ label: 'Главная', href: '/' }, { label: 'Контакты' }]}
  />

  <section class="py-10 sm:py-14">
    <div class="section-container">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mb-12">
        <div class="p-6 rounded-xl metal-border bg-white text-center">
          <div class="rivet mx-auto mb-3" aria-hidden="true"></div>
          <h2 class="font-semibold text-steel-900 text-sm">Телефон</h2>
          <a href={SITE.phoneHref} class="mt-2 block text-brand font-medium text-lg hover:underline">{SITE.phone}</a>
        </div>
        <div class="p-6 rounded-xl metal-border bg-white text-center">
          <div class="rivet mx-auto mb-3" aria-hidden="true"></div>
          <h2 class="font-semibold text-steel-900 text-sm">Email</h2>
          <a href={`mailto:${SITE.email}`} class="mt-2 block text-brand font-medium hover:underline break-all">{SITE.email}</a>
        </div>
        <div class="p-6 rounded-xl metal-border bg-white text-center">
          <div class="rivet mx-auto mb-3" aria-hidden="true"></div>
          <h2 class="font-semibold text-steel-900 text-sm">Адрес</h2>
          <p class="mt-2 text-steel-600">{SITE.address}</p>
        </div>
        <div class="p-6 rounded-xl metal-border bg-white text-center">
          <div class="rivet mx-auto mb-3" aria-hidden="true"></div>
          <h2 class="font-semibold text-steel-900 text-sm">Режим работы</h2>
          <p class="mt-2 text-steel-600">{SITE.workHours}</p>
        </div>
      </div>

      <div class="rounded-xl metal-border bg-white p-6 sm:p-8 mb-12">
        <h2 class="text-xl font-bold text-steel-900 mb-6">Реквизиты компании</h2>
        <dl class="grid grid-cols-1 sm:grid-cols-2 gap-x-8 gap-y-4 text-sm">
          <div>
            <dt class="text-steel-500">Полное наименование</dt>
            <dd class="mt-1 font-medium text-steel-900">{SITE.legalNameFull}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Сокращённое наименование</dt>
            <dd class="mt-1 font-medium text-steel-900">{SITE.legalName}</dd>
          </div>
          <div class="sm:col-span-2">
            <dt class="text-steel-500">Юридический адрес</dt>
            <dd class="mt-1 font-medium text-steel-900">{SITE.address}</dd>
          </div>
          <div>
            <dt class="text-steel-500">ИНН</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.inn}</dd>
          </div>
          <div>
            <dt class="text-steel-500">КПП</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.kpp}</dd>
          </div>
          <div>
            <dt class="text-steel-500">ОГРН</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.ogrn}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Система налогообложения</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.taxation}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Ставка НДС</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.vat}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Генеральный директор</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.director}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Банк</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.bank}</dd>
          </div>
          <div>
            <dt class="text-steel-500">БИК</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.bik}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Расчётный счёт</dt>
            <dd class="mt-1 font-medium text-steel-900 font-mono text-xs sm:text-sm">{REQUISITES.account}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Корреспондентский счёт</dt>
            <dd class="mt-1 font-medium text-steel-900 font-mono text-xs sm:text-sm">{REQUISITES.corrAccount}</dd>
          </div>
          <div class="sm:col-span-2">
            <dt class="text-steel-500">Идентификатор ЭДО (Контур.Диадок)</dt>
            <dd class="mt-1 font-medium text-steel-900 font-mono text-xs break-all">{REQUISITES.edo}</dd>
          </div>
          <div>
            <dt class="text-steel-500">Рег. номер в ЕИС</dt>
            <dd class="mt-1 font-medium text-steel-900">{REQUISITES.eis}</dd>
          </div>
        </dl>
      </div>

      <div class="rounded-xl steel-section p-6 sm:p-8 max-w-2xl">
        <h2 class="text-lg font-bold text-white">Быстрый способ получить расчёт</h2>
        <p class="mt-2 text-steel-300 text-sm leading-relaxed">
          Отправьте спецификацию на {SITE.email} или позвоните {SITE.phone}.
          Менеджер проверит позиции и подготовит коммерческое предложение.
        </p>
        <div class="mt-4 flex flex-col sm:flex-row gap-3">
          <a href={`mailto:${SITE.email}`}
            class="inline-flex items-center justify-center px-6 py-3 bg-brand text-white font-semibold rounded-lg hover:bg-brand-light transition-colors min-h-[44px]">
            Написать на email
          </a>
          <a href={SITE.phoneHref}
            class="inline-flex items-center justify-center px-6 py-3 metal-surface metal-border text-steel-800 font-semibold rounded-lg hover:brightness-105 transition-all min-h-[44px]">
            Позвонить
          </a>
        </div>
      </div>
    </div>
  </section>

  <RequestFormBlock title="Оставить заявку" />
</BaseLayout>
